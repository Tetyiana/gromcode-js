const getSubArray = (arr, numberOfElements) => {
  // put your code here
  // let n = numberOfElements - 1;
  const subArray = arr.slice(0, numberOfElements);
  return subArray;
};
getSubArray([11, 4, 8, 3], 2);

